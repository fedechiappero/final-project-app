{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pedido creation</h1>

    {{ include(':pedido:form.html.twig') }}

    <ul>
        <li>
            <a href="{{ path('pedido_index') }}">Back to the list</a>
        </li>
    </ul>

    <script>
        $(document).ready(function() {
            $('#frmpedido').attr('action', "{{ path('pedido_new') }}");
        });
    </script>
{% endblock %}
