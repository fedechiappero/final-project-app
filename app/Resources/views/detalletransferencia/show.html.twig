{% extends 'base.html.twig' %}

{% block body %}
    <h1>DetalleTransferencia</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleTransferencia.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detalletransferencia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detalletransferencia_edit', { 'id': detalleTransferencia.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
