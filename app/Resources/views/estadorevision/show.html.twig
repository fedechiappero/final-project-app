{% extends 'base.html.twig' %}

{% block body %}
    <h1>Estadorevision</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ estadoRevision.nombre }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ estadoRevision.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('estadorevision_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('estadorevision_edit', { 'id': estadoRevision.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
