{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sitiotarea</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ sitioTarea.nombre }}</td>
            </tr>
            <tr>
                <th>Unidad</th>
                <td>{{ sitioTarea.unidad }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ sitioTarea.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('sitiotarea_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('sitiotarea_edit', { 'id': sitioTarea.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
