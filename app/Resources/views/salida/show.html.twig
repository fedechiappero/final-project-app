{% extends 'base.html.twig' %}

{% block body %}
    <h1>Salida</h1>

    <table>
        <tbody>
            <tr>
                <th>Fecha</th>
                <td>{% if salida.fecha %}{{ salida.fecha|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ salida.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('salida_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('salida_edit', { 'id': salida.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
