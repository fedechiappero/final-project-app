{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contratistarubro</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contratistaRubro.id }}</td>
            </tr>
        <tr>
            <th>Razon social</th>
            <td>{{ contratistaRubro.razonSocial }}</td>
        </tr>
        <tr>
            <th>Rubro</th>
            <td>{{ contratistaRubro.nombre }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('contratistarubro_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('contratistarubro_edit', { 'id': contratistaRubro.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
