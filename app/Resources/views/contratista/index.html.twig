{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contratistas list</h1>

    <table>
        <thead>
            <tr>
                <th>Razonsocial</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contratista in contratistas %}
            <tr>
                <td><a href="{{ path('contratista_show', { 'id': contratista.id }) }}">{{ contratista.razonSocial }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('contratista_show', { 'id': contratista.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('contratista_edit', { 'id': contratista.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('contratista_new') }}">Create a new contratista</a>
        </li>
    </ul>
{% endblock %}
