{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tiporevision</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoRevision.nombre }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tipoRevision.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tiporevision_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tiporevision_edit', { 'id': tipoRevision.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
