{% extends 'base.html.twig' %}

{% block body %}
    <h1>Detallecomprobante</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleComprobante.id }}</td>
            </tr>
            <tr>
                <th>Idcomprobante</th>
                <td>{{ detalleComprobante.idComprobante }}</td>
            </tr>
            <tr>
                <th>Idproducto</th>
                <td>{{ detalleComprobante.idProducto }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ detalleComprobante.cantidad }}</td>
            </tr>
            <tr>
                <th>Preciounitario</th>
                <td>{{ detalleComprobante.precioUnitario }}</td>
            </tr>
            <tr>
                <th>Cantidadrecibida</th>
                <td>{{ detalleComprobante.cantidadRecibida }}</td>
            </tr>
            <tr>
                <th>Iva</th>
                <td>{{ detalleComprobante.iva }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detallecomprobante_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detallecomprobante_edit', { 'id': detalleComprobante.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
