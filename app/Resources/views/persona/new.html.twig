{% extends 'base.html.twig' %}

{% block body %}
    <h1>Persona creation</h1>

    {{ include(':persona:form.html.twig') }}

    <ul>
        <li>
            <a href="{{ path('persona_index') }}">Back to the list</a>
        </li>
    </ul>


    <script>
        $(document).ready(function() {
            $('#frmpersona').attr('action', "{{ path('persona_new') }}");
        });
    </script>
{% endblock %}
