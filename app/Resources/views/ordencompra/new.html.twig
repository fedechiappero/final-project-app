{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ordencompra creation</h1>

    {{ include(':ordencompra:form.html.twig') }}

    <ul>
        <li>
            <a href="{{ path('ordencompra_index') }}">Back to the list</a>
        </li>
    </ul>

    <script>
        $(document).ready(function() {
            $('#frmordencompra').attr('action', "{{ path('ordencompra_new') }}");
        });
    </script>
{% endblock %}
