{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tipoinmueble</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoInmueble.nombre }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ tipoInmueble.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipoinmueble_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipoinmueble_edit', { 'id': tipoInmueble.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
