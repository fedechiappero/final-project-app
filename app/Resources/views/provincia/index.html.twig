{% extends 'base.html.twig' %}

{% block body %}
    <h1>Provincias list</h1>

    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for provincia in provincias %}
            <tr>
                <td><a href="{{ path('provincia_show', { 'id': provincia.id }) }}">{{ provincia.nombre }}</a></td>
                <td>{{ provincium.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('provincia_show', { 'id': provincia.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('provincia_edit', { 'id': provincia.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('provincia_new') }}">Create a new provincium</a>
        </li>
    </ul>
{% endblock %}
