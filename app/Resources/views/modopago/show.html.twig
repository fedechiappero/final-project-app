{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modopago</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ modoPago.nombre }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ modoPago.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('modopago_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('modopago_edit', { 'id': modoPago.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
