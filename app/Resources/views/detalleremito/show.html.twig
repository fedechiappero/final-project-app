{% extends 'base.html.twig' %}

{% block body %}
    <h1>Detalleremito</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleRemito.id }}</td>
            </tr>
            <tr>
                <th>Idproducto</th>
                <td>{{ detalleRemito.idProducto }}</td>
            </tr>
            <tr>
                <th>Idremito</th>
                <td>{{ detalleRemito.idRemito }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detalleremito_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detalleremito_edit', { 'id': detalleRemito.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
