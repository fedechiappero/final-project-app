{% extends 'base.html.twig' %}

{% block body %}
    <h1>Caducidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Cantidadmeses</th>
                <td>{{ caducidad.cantidadMeses }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('caducidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('caducidad_edit', { 'id': caducidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
